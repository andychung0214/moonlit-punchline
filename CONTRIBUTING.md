# 貢獻指南

感謝你替月下冷梗旅店送來新的房客。請讓每次變更保持小而清楚，並在提交前完成對應測試。

## 開發流程

1. 從最新基準分支建立工作分支。
2. 功能使用 `feature/xxx`，修正使用 `fix/xxx`，維護使用 `chore/xxx`。
3. 先寫一個會因缺少行為而失敗的測試。
4. 實作最小變更，使測試通過。
5. 執行 `node --test` 與相關瀏覽器手動測試。
6. 確認 `git diff --check` 沒有空白錯誤。

## 題庫內容規範

每則笑話必須：

- 適合親子、校園與辦公室。
- 不含髒話、成人暗示、仇恨、歧視、身體羞辱或針對弱勢的笑點。
- 具有唯一穩定 ID、分類、問題、答案、三個干擾選項、來源地區、註解與 1–3 難度。
- 三個干擾選項互不重複，也不得包含正確答案。
- 跨文化內容在繁體中文中仍能理解，並說明文化或語言脈絡。
- 不複製受著作權保護的長篇笑話或付費題庫。

新增或移除題目時，需同步調整六類各 12 題的產品規格與測試；若首版規模不變，請以一換一方式修改。

## 程式碼風格

- 只使用 HTML、CSS 與 Vanilla JavaScript ES Modules。
- 函式與變數採具語意英文命名。
- 狀態計算放在純函式，不在 UI 內直接計分。
- 題庫文字插入 HTML 前必須使用 `escapeHtml`。
- 所有新互動需有可見焦點、原生鍵盤操作與至少 44 × 44 CSS px 的觸控目標。
- 中文文件、註解及 UI 文案使用繁體中文並遵守專案名詞規範。
- 不提交 `.env`、憑證、token、私人金鑰、建構快取或編輯器個人設定。

## Commit 訊息

採用 [Conventional Commits 1.0.0](https://www.conventionalcommits.org/)；描述必須使用繁體中文：

```text
<type>[optional scope]: <繁體中文描述>
```

範例：

```text
feat(gallery): 加入收藏房牌篩選
fix(storage): 修正損壞紀錄的降級流程
test(jokes): 補充題庫唯一性驗證
docs: 更新靜態網站部署說明
```

Breaking change 使用 `!` 或 `BREAKING CHANGE:` footer 清楚標記。

## 提交前清單

- [ ] `node --test` 全部通過
- [ ] `git diff --check` 無錯誤
- [ ] 相關桌機、平板及手機流程已驗證
- [ ] 鍵盤與減少動態效果沒有退化
- [ ] 未加入私人資訊或未授權素材
